export type Profile = {
  name: string;
  brandName: string;
  tagline: string;
  genres: string;
  siteTitle: string;
  siteDescription: string;
  trailerUrl?: string;
  trailerUrlLandscape?: string;
};
